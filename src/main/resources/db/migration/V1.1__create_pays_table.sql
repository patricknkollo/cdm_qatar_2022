CREATE table Pays (
                       paysid int NOT NULL AUTO_INCREMENT,
                       nom varchar(255),
                       habitants int,
                       cdms int,
                       superficie int
);
CREATE table Club (
                      clubid int NOT NULL AUTO_INCREMENT,
                      nom varchar(255),
                      ville varchar(255),
                      pays varchar(255),
                      paysid int
);